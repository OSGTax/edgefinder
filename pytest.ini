[pytest]
markers =
    integration: marks tests that hit real external APIs (deselect with '-m "not integration"')
filterwarnings =
    ignore::DeprecationWarning:modules.scanner
    ignore::DeprecationWarning:modules.database
